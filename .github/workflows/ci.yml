name: CI
on: [push]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run a sample command
        run: echo "Hello from self-hosted runner!"
